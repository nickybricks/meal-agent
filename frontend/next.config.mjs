/** @type {import('next').NextConfig} */
const nextConfig = {
  // Backend URL for server-side API calls (overridable via env)
  // Client-side calls use NEXT_PUBLIC_API_URL
};

export default nextConfig;
