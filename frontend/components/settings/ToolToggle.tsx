/**
 * ToolToggle.tsx — Enable/disable individual agent tools.
 *
 * Props:
 *   enabledTools: string[]
 *   onChange: (tools: string[]) => void
 *
 * Renders one toggle switch per tool:
 *   search_recipes, get_user_profile, save_preference,
 *   substitute_ingredient, generate_meal_plan
 *
 * Each shows the tool name plus a one-line description of what it does.
 * State is persisted in localStorage and injected into every ChatRequest.
 */

// TODO: implement in Phase 3
export default function ToolToggle() {
  return null;
}
