/**
 * EditPrompt.tsx — Inline edit form that replaces a user message bubble.
 *
 * Props:
 *   originalText: string
 *   checkpointId: string
 *   onConfirm: (checkpointId: string, newText: string) => void
 *   onCancel: () => void
 *
 * Renders a textarea pre-filled with originalText plus Confirm / Cancel buttons.
 * Confirm calls onConfirm with the edited text; Cancel restores the original bubble.
 */

// TODO: implement in Phase 3
export default function EditPrompt() {
  return null;
}
