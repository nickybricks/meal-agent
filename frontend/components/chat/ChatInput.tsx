/**
 * ChatInput.tsx — Message composition bar at the bottom of ChatWindow.
 *
 * Props:
 *   onSend: (text: string) => void
 *   disabled: boolean  — true while a response is loading
 *
 * Features:
 * - Auto-growing textarea (max 5 lines before scroll)
 * - Submit on Enter (Shift+Enter for newline)
 * - Send button with loading spinner when disabled
 * - Clears input after send
 */

// TODO: implement in Phase 3
export default function ChatInput() {
  return null;
}
