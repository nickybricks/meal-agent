/**
 * FeedbackButtons.tsx — Thumbs up / thumbs down row shown below assistant messages.
 *
 * Props:
 *   checkpointId: string
 *   onFeedback: (checkpointId: string, rating: 1 | 5) => void
 *
 * Shows two icon buttons. On click:
 * - Calls onFeedback with rating 5 (thumbs up) or 1 (thumbs down)
 * - Highlights the selected button and disables both to prevent double-voting
 */

// TODO: implement in Phase 3
export default function FeedbackButtons() {
  return null;
}
