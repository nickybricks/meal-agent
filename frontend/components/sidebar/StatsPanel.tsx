/**
 * StatsPanel.tsx — Session statistics shown at the bottom of the sidebar.
 *
 * Props:
 *   totalTokens: number     — cumulative tokens used this session
 *   messageCount: number    — number of exchanges
 *   likeCount: number       — thumbs up given this session
 *   dislikeCount: number    — thumbs down given this session
 *
 * Displays as a small grid of labelled numbers.
 * Resets when the user switches profiles or starts a new session.
 */

// TODO: implement in Phase 3
export default function StatsPanel() {
  return null;
}
