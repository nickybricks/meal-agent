/**
 * PersonalityToggle.tsx — Three-way toggle for agent personality.
 *
 * Props:
 *   value: Personality        — "friendly" | "professional" | "concise"
 *   onChange: (p: Personality) => void
 *
 * Rendered as a segmented button group.
 * The selected personality is sent in every ChatRequest and controls which
 * system prompt prefix the backend injects (see config.PERSONALITY_PROMPTS).
 */

// TODO: implement in Phase 3
export default function PersonalityToggle() {
  return null;
}
